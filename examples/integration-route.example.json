{
  "route_id": "example-service-work-workspace",
  "system": "example-service",
  "credential_ref_name": "<credential-ref>",
  "default_contexts": ["primary"],
  "access_probe_id": "example-service-work-readiness",
  "notes": "Write lane. Reprobe before any write. A ready default route for this system is not evidence for this named route. The credential slot holds a reference only, never a value; the four sanctioned reference styles are a provider-native credential store for a named identity, an environment-variable name, a path to a permission-restricted environment file, and a named reference into an OS credential store."
}
