{
  "schema": "probe-registry/v1",
  "probes": [
    {
      "probe_id": "example-service-work-readiness",
      "command": "<workspace-root>/scripts/example_service_capability_probe.py --route example-service-work-workspace",
      "expected_signal": "prints a structured summary reporting ok plus which route and which identity answered, without printing token values; a missing credential returns a structured blocked result rather than an exception",
      "safe_lane": "local-read-network-read",
      "default_ttl_ms": 3600000,
      "scope": "example-service-work-workspace",
      "notes": "Strictly non-mutating. The mutating subcommands of the tool being probed are asserted absent from this command string by registry-contract tests."
    },
    {
      "probe_id": "control-plane-self-consistency",
      "command": "<workspace-root>/scripts/workspace_validate.sh",
      "expected_signal": "topology contract check and the workspace test modules pass; exits non-zero on any missing contract, registry, or control file",
      "safe_lane": "local-read",
      "default_ttl_ms": 86400000,
      "scope": "control-plane",
      "notes": "Control-plane self-consistency is registered as a capability with a probe id like any integration. Local credential and readiness checks carry the long time-to-live; network- or state-volatile checks carry the short one."
    }
  ]
}
