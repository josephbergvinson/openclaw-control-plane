# USER.md — Durable Preferences and Context (the operator)

Role boundary:
- Durable preferences, recurring context, and interpretation aids only.
- This file does not define operational authority, source-of-truth rules, approval policy, or current runtime facts.
- Dated context should be labeled as dated context, not treated as timeless fact.
- Keep this file focused on always-useful operating context. Sensitive biographical, health, family, financial, or similarly private material belongs in conditional private memory sources rather than the default bootstrap surface.

## Stable context
- the operator; adapt this line to your role/context.
- Core recurring domains: adapt this line to your recurring work domains.
- Works remote, mostly independently, with targeted collaboration.
- Add other recurring professional context here if useful.
- Additional high-sensitivity context is intentionally kept out of the default operating profile; use private, untracked `memory/<conditional-context>.md` sources only when the task materially depends on them.

## Durable working preferences
Workflow
- Memo-first and async.
- Prefer explicit assumptions, clear tradeoffs, and model-first reasoning.
- Strong preference for tight experiments over vague ideation.
- For selected projects, Trello should function as the human control plane via task-sync checkpoints; repo/worktree/commit/test/artifact state remains the technical source of truth.
- GitHub should only be surfaced when the task explicitly concerns the DO-facing `example-project-app` code lane or the operator explicitly mentions GitHub; otherwise default to the local authoritative path and keep GitHub out of the conversation.

Communication
- Direct, concise, structured, technically clear.
- Minimal fluff; no emojis.
- Async by default; avoid ritual meetings.
- For technical documents: prioritize direct clarity over literary cadence.
- For essays or reflective writing: analytical, unsentimental, competence-forward tone with explicit causality.

Decision posture
- Evidence / EV based, with second-order effects in view.
- Willing to test contrarian hypotheses under time-boxed safety rails.
- Prefers assumptions and decision criteria to be visible.

Thinking / learning
- Model-driven, dialectical, simulation-friendly.
- Comfortable with rhizomatic exploration when mapping a space, followed by compression into a usable model and decision tree.

## Preferred tools and output forms
- One-page memos with TL;DR, decision trees, and explicit assumptions.
- Dashboards, spreadsheets, Monte Carlo, backtests, and simulation-friendly framing.
- PRD-lite structures for decisions, risks, and tradeoffs.

MacBook-facing path preference:
- When giving user-facing run or handoff paths, prefer `<OPERATOR_HANDOFF_ROOT>`.
- `<LEGACY_HANDOFF_ROOT>` is deprecated.
- For a project-specific handoff, use `<OPERATOR_HANDOFF_ROOT>/<PROJECT_HANDOFF>` and provide the quick-launch command from that refreshed copy.
- This is a convenience preference for operator-facing paths. It does not by itself declare canonical source-of-truth authority.

## Common output types
- Tokenomics reports with assumptions, sensitivities, attack surfaces, and explicit tradeoffs.
- Governance proposals with motivation, spec, impact, risks, implementation, rollback, and FAQ.
- Data-analysis memos and decision briefs.
- Code-coordination briefs for coding agents.
- Admin support for email, calendar, and document maintenance.
- Finance, tax, and accounting workflows with high sensitivity.
